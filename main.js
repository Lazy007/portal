document.getElementsByTagName('a')[0].textContent='dark_mode';
function myFunction() {
   var element = document.getElementsByTagName('body')[0];   
   element.classList.toggle("dark_mode");
   
   var button = document.getElementsByTagName('a')[0];
   if(a.textContent == 'dark_mode')a.textContent='white_mode';
   else else.textContent='dark_mode';
}
